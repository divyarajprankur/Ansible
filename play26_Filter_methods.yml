---
- hosts: localhost
  vars:
    a : "ansible divyaraj"
  gather_facts: false
  tasks:
    - ansible.builtin.debug:
        msg:  
          - " a value is : {{a}}"
          - " a in upper: {{a|upper}}"
          - " a in lover: {{a|lower}}"
          - " {{'hello' | upper}}"
